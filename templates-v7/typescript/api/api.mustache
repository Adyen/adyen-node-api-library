{{>licenseInfo}}
import getJsonResponse from "../../helpers/getJsonResponse";
import Service from "../../service";
import Client from "../../client";
import { IRequest } from "../../typings/requestOptions";
import Resource from "../resource";

import { ObjectSerializer } from "../../typings/{{serviceName}}/objectSerializer";
{{#imports}}
import { {{classname}} } from "../../typings/{{serviceName}}/models";
{{/imports}}
{{#imports}}
void {{classname}};
{{/imports}}
{{#operations}}

/**
 * API handler for {{classname}}
 */
{{#useInversify}}
@injectable()
{{/useInversify}}
export class {{classname}} extends Service {

    private readonly API_BASEPATH: string = "{{{basePath}}}";
    private baseUrl: string;

    public constructor(client: Client){
        super(client);
        this.baseUrl = this.createBaseUrl(this.API_BASEPATH);
    }

    {{#operation}}
{{>api_summary}}
    public async {{#vendorExtensions.x-methodName}}{{.}}{{/vendorExtensions.x-methodName}}{{^vendorExtensions.x-methodName}}{{nickname}}{{/vendorExtensions.x-methodName}}({{#pathParams}}{{paramName}}: {{{dataType}}}, {{/pathParams}}{{#bodyParams}}{{paramName}}: {{{dataType}}}, {{/bodyParams}}{{#formParams}}{{#required}}{{paramName}}: {{#isFile}}import("../../typings/httpFile").HttpFile{{/isFile}}{{^isFile}}{{{dataType}}}{{/isFile}}, {{/required}}{{/formParams}}{{#queryParams}}{{#required}}{{paramName}}: {{{dataType}}}, {{/required}}{{/queryParams}}{{#formParams}}{{^required}}{{paramName}}?: {{#isFile}}import("../../typings/httpFile").HttpFile{{/isFile}}{{^isFile}}{{{dataType}}}{{/isFile}}, {{/required}}{{/formParams}}{{#queryParams}}{{^required}}{{paramName}}?: {{{dataType}}}, {{/required}}{{/queryParams}}requestOptions?: IRequest.Options): Promise<{{#returnProperty}}{{openApiType}}{{/returnProperty}}{{^returnProperty}}void{{/returnProperty}}> {
        const endpoint = `${this.baseUrl}{{{path}}}`{{#pathParams}}
            .replace("{" + "{{baseName}}" + "}", encodeURIComponent(String({{paramName}}))){{/pathParams}};
        const resource = new Resource(this, endpoint);
        
{{#formParams}}
{{#required}}
        if ({{paramName}} === null || {{paramName}} === undefined) {
            throw new Error("Required parameter {{paramName}} was null or undefined");
        }
{{/required}}
{{/formParams}}
{{#bodyParam}}
        const request: {{{dataType}}} = ObjectSerializer.serialize({{paramName}}, "{{{dataType}}}");
{{/bodyParam}}
{{#hasFormParams}}
        const contentTypes = [{{#consumes}}"{{{mediaType}}}"{{^-last}}, {{/-last}}{{/consumes}}];
        const requestForm = await import("../../helpers/requestForm");
        const form = requestForm.createRequestForm(contentTypes);
{{#formParams}}
{{#isArray}}
        if ({{paramName}} != null) {
            {{paramName}}.forEach((element) => {
{{#isFile}}
                if ("getBuffer" in form) {
                    form.append("{{baseName}}", element.data, { filename: element.name });
                }
{{/isFile}}
{{^isFile}}
                form.append("{{baseName}}", String(element));
{{/isFile}}
            });
        }
{{/isArray}}
{{^isArray}}
        if ({{paramName}} != null) {
{{#isFile}}
            if ("getBuffer" in form) {
                form.append("{{baseName}}", {{paramName}}.data, { filename: {{paramName}}.name });
            }
{{/isFile}}
{{^isFile}}
            form.append("{{baseName}}", String({{paramName}}));
{{/isFile}}
        }
{{/isArray}}
{{/formParams}}
        const { request, headers } = requestForm.finalizeRequestForm(form, requestOptions?.headers);
{{/hasFormParams}}
{{#hasQueryParams}}
        const hasDefinedQueryParams = {{#queryParams}}{{paramName}}{{^-last}} ?? {{/-last}}{{/queryParams}};
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
{{#queryParams}}
            if({{paramName}}) requestOptions.params["{{paramName}}"] = {{paramName}}{{#isDateTime}}.toISOString(){{/isDateTime}};
{{/queryParams}}
        }
{{/hasQueryParams}}
        {{#returnProperty}}const response = {{/returnProperty}}await getJsonResponse<{{#bodyParam}}{{{dataType}}}{{/bodyParam}}{{#hasFormParams}}string | Buffer{{/hasFormParams}}{{^bodyParam}}{{^hasFormParams}}string{{/hasFormParams}}{{/bodyParam}}, {{#returnProperty}}{{openApiType}}{{/returnProperty}}{{^returnProperty}}void{{/returnProperty}}>(
            resource,
            {{#bodyParam}}request{{/bodyParam}}{{#hasFormParams}}request{{/hasFormParams}}{{^bodyParam}}{{^hasFormParams}}""{{/hasFormParams}}{{/bodyParam}},
            { ...requestOptions, {{#hasFormParams}}headers, {{/hasFormParams}}method: "{{httpMethod}}" }
        );
        {{#returnProperty}}

        return ObjectSerializer.deserialize(response, "{{{openApiType}}}");
        {{/returnProperty}}
    }

    {{/operation}}
}
{{/operations}}
