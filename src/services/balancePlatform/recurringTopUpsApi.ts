/*
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit this class manually.
 */


import getJsonResponse from "../../helpers/getJsonResponse";
import Service from "../../service";
import Client from "../../client";
import { IRequest } from "../../typings/requestOptions";
import Resource from "../resource";

import { ObjectSerializer } from "../../typings/balancePlatform/objectSerializer";
import { CreateRecurringTopUp } from "../../typings/balancePlatform/models";
import { PatchableCreateRecurringTopUp } from "../../typings/balancePlatform/models";
import { RecurringTopUp } from "../../typings/balancePlatform/models";
import { RecurringTopUpsResult } from "../../typings/balancePlatform/models";

/**
 * API handler for RecurringTopUpsApi
 */
export class RecurringTopUpsApi extends Service {

    private readonly API_BASEPATH: string = "https://balanceplatform-api-test.adyen.com/bcl/v2";
    private baseUrl: string;

    public constructor(client: Client){
        super(client);
        this.baseUrl = this.createBaseUrl(this.API_BASEPATH);
    }

    /**
    * @summary Create recurring top up
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param createRecurringTopUp {@link CreateRecurringTopUp } 
    * @param requestOptions {@link IRequest.Options }
    * @return {@link RecurringTopUp }
    */
    public async createRecurringTopUp(balanceAccountId: string, createRecurringTopUp: CreateRecurringTopUp, requestOptions?: IRequest.Options): Promise<RecurringTopUp> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/recurringTopUps`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)));
        const resource = new Resource(this, endpoint);
        
        const request: CreateRecurringTopUp = ObjectSerializer.serialize(createRecurringTopUp, "CreateRecurringTopUp");
        const response = await getJsonResponse<CreateRecurringTopUp, RecurringTopUp>(
            resource,
            request,
            { ...requestOptions, method: "POST" }
        );

        return ObjectSerializer.deserialize(response, "RecurringTopUp");
    }

    /**
    * @summary Delete a recurring top up
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param topUpId {@link string } The unique identifier of the recurring top-up you want to delete.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link void }
    */
    public async deleteRecurringTopUp(balanceAccountId: string, topUpId: string, requestOptions?: IRequest.Options): Promise<void> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/recurringTopUps/{topUpId}`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)))
            .replace("{" + "topUpId" + "}", encodeURIComponent(String(topUpId)));
        const resource = new Resource(this, endpoint);
        
        await getJsonResponse<string, void>(
            resource,
            "",
            { ...requestOptions, method: "DELETE" }
        );
    }

    /**
    * @summary Get recurring top ups for a specific balanceAccountId
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param requestOptions {@link IRequest.Options }
    * @param limit {@link number } The number of items to return per page. Value must be between 1 and 100.
    * @param cursor {@link string } The cursor used for pagination. Required if you want to see the next or previous page of results.
    * @return {@link RecurringTopUpsResult }
    */
    public async getRecurringTopUps(balanceAccountId: string, limit?: number, cursor?: string, requestOptions?: IRequest.Options): Promise<RecurringTopUpsResult> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/recurringTopUps`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)));
        const resource = new Resource(this, endpoint);
        
        const hasDefinedQueryParams = limit ?? cursor;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(limit) requestOptions.params["limit"] = limit;
            if(cursor) requestOptions.params["cursor"] = cursor;
        }
        const response = await getJsonResponse<string, RecurringTopUpsResult>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "RecurringTopUpsResult");
    }

    /**
    * @summary Update a RecurringTopUp
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param topUpId {@link string } The unique identifier of the recurring top-up you want to update.
    * @param patchableCreateRecurringTopUp {@link PatchableCreateRecurringTopUp } 
    * @param requestOptions {@link IRequest.Options }
    * @return {@link RecurringTopUp }
    */
    public async updateRecurringTopUps(balanceAccountId: string, topUpId: string, patchableCreateRecurringTopUp: PatchableCreateRecurringTopUp, requestOptions?: IRequest.Options): Promise<RecurringTopUp> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/recurringTopUps/{topUpId}`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)))
            .replace("{" + "topUpId" + "}", encodeURIComponent(String(topUpId)));
        const resource = new Resource(this, endpoint);
        
        const request: PatchableCreateRecurringTopUp = ObjectSerializer.serialize(patchableCreateRecurringTopUp, "PatchableCreateRecurringTopUp");
        const response = await getJsonResponse<PatchableCreateRecurringTopUp, RecurringTopUp>(
            resource,
            request,
            { ...requestOptions, method: "PATCH" }
        );

        return ObjectSerializer.deserialize(response, "RecurringTopUp");
    }

}
