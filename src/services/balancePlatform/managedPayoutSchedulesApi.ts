/*
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit this class manually.
 */


import getJsonResponse from "../../helpers/getJsonResponse";
import Service from "../../service";
import Client from "../../client";
import { IRequest } from "../../typings/requestOptions";
import Resource from "../resource";

import { ObjectSerializer } from "../../typings/balancePlatform/objectSerializer";
import { BalanceAccountConfiguration } from "../../typings/balancePlatform/models";
import { BalanceAccountConfigurationRequest } from "../../typings/balancePlatform/models";
import { BalanceAccountConfigurationUpdate } from "../../typings/balancePlatform/models";
import { BalanceAccountConfigurations } from "../../typings/balancePlatform/models";
import { BalancePlatformConfiguration } from "../../typings/balancePlatform/models";
import { BalancePlatformConfigurations } from "../../typings/balancePlatform/models";
import { ExecutionResult } from "../../typings/balancePlatform/models";
import { PayoutScheduleExecutions } from "../../typings/balancePlatform/models";

/**
 * API handler for ManagedPayoutSchedulesApi
 */
export class ManagedPayoutSchedulesApi extends Service {

    private readonly API_BASEPATH: string = "https://balanceplatform-api-test.adyen.com/bcl/v2";
    private baseUrl: string;

    public constructor(client: Client){
        super(client);
        this.baseUrl = this.createBaseUrl(this.API_BASEPATH);
    }

    /**
    * @summary Apply a managed payout schedule to a balance account
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param balanceAccountConfigurationRequest {@link BalanceAccountConfigurationRequest } 
    * @param requestOptions {@link IRequest.Options }
    * @return {@link BalanceAccountConfiguration }
    */
    public async applyManagedSchedule(balanceAccountId: string, balanceAccountConfigurationRequest: BalanceAccountConfigurationRequest, requestOptions?: IRequest.Options): Promise<BalanceAccountConfiguration> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/payoutSchedules`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)));
        const resource = new Resource(this, endpoint);
        
        const request: BalanceAccountConfigurationRequest = ObjectSerializer.serialize(balanceAccountConfigurationRequest, "BalanceAccountConfigurationRequest");
        const response = await getJsonResponse<BalanceAccountConfigurationRequest, BalanceAccountConfiguration>(
            resource,
            request,
            { ...requestOptions, method: "POST" }
        );

        return ObjectSerializer.deserialize(response, "BalanceAccountConfiguration");
    }

    /**
    * @summary Delete a managed payout schedule on a balance account
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param id {@link string } The unique identifier of the payout schedule applied to the balance account.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link void }
    */
    public async deleteBalanceAccountManagedSchedule(balanceAccountId: string, id: string, requestOptions?: IRequest.Options): Promise<void> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/payoutSchedules/{id}`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        await getJsonResponse<string, void>(
            resource,
            "",
            { ...requestOptions, method: "DELETE" }
        );
    }

    /**
    * @summary Get a specific managed payout schedule on a balance account
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param id {@link string } The unique identifier of the payout schedule for the balance account.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link BalanceAccountConfiguration }
    */
    public async getBalanceAccountManagedScheduleById(balanceAccountId: string, id: string, requestOptions?: IRequest.Options): Promise<BalanceAccountConfiguration> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/payoutSchedules/{id}`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const response = await getJsonResponse<string, BalanceAccountConfiguration>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "BalanceAccountConfiguration");
    }

    /**
    * @summary Get all managed payout schedules on a balance account
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param requestOptions {@link IRequest.Options }
    * @param currency {@link string } The three-character [ISO currency code](https://docs.adyen.com/development-resources/currency-codes) of the currency used in the payout schedule.
    * @param cursor {@link string } The &#x60;cursor&#x60; returned in the links of the previous response.
    * @param limit {@link number } The number of items returned per page, maximum of 100 items. By default, the response returns 10 items per page.
    * @return {@link BalanceAccountConfigurations }
    */
    public async getBalanceAccountManagedSchedules(balanceAccountId: string, currency?: string, cursor?: string, limit?: number, requestOptions?: IRequest.Options): Promise<BalanceAccountConfigurations> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/payoutSchedules`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)));
        const resource = new Resource(this, endpoint);
        
        const hasDefinedQueryParams = currency ?? cursor ?? limit;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(currency) requestOptions.params["currency"] = currency;
            if(cursor) requestOptions.params["cursor"] = cursor;
            if(limit) requestOptions.params["limit"] = limit;
        }
        const response = await getJsonResponse<string, BalanceAccountConfigurations>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "BalanceAccountConfigurations");
    }

    /**
    * @summary Get a specific managed payout schedule on your balance platform
    * @param balancePlatformId {@link string } The unique identifier of the balance platform.
    * @param id {@link string } The unique identifier of the payout configuration for your balance platform.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link BalancePlatformConfiguration }
    */
    public async getBalancePlatformManagedScheduleById(balancePlatformId: string, id: string, requestOptions?: IRequest.Options): Promise<BalancePlatformConfiguration> {
        const endpoint = `${this.baseUrl}/balancePlatforms/{balancePlatformId}/payoutSchedules/{id}`
            .replace("{" + "balancePlatformId" + "}", encodeURIComponent(String(balancePlatformId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const response = await getJsonResponse<string, BalancePlatformConfiguration>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "BalancePlatformConfiguration");
    }

    /**
    * @summary Get all managed payout schedules on your balance platform
    * @param balancePlatformId {@link string } The unique identifier of the balance platform.
    * @param requestOptions {@link IRequest.Options }
    * @param countryCode {@link string } The two-letter [ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the country to which the payout configuration applies.
    * @param currency {@link string } The three-character [ISO currency code](https://docs.adyen.com/development-resources/currency-codes) of the currency used in the payout configuration.
    * @return {@link BalancePlatformConfigurations }
    */
    public async getBalancePlatformManagedSchedules(balancePlatformId: string, countryCode?: string, currency?: string, requestOptions?: IRequest.Options): Promise<BalancePlatformConfigurations> {
        const endpoint = `${this.baseUrl}/balancePlatforms/{balancePlatformId}/payoutSchedules`
            .replace("{" + "balancePlatformId" + "}", encodeURIComponent(String(balancePlatformId)));
        const resource = new Resource(this, endpoint);
        
        const hasDefinedQueryParams = countryCode ?? currency;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(countryCode) requestOptions.params["countryCode"] = countryCode;
            if(currency) requestOptions.params["currency"] = currency;
        }
        const response = await getJsonResponse<string, BalancePlatformConfigurations>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "BalancePlatformConfigurations");
    }

    /**
    * @summary View executions of a managed payout schedule
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param id {@link string } The unique identifier of the payout schedule on the balance account.
    * @param requestOptions {@link IRequest.Options }
    * @param offset {@link number } (Required) The page number to be returned.  Default: **1**
    * @param results {@link Array&lt;ExecutionResult&gt; } Contains a list of payout statuses. If included, the response returns only executed payouts that currently have one of the specified statuses.  Possible statuses: - **succeeded**: The payout was sent successfully. - **failed**: The payout was not sent due to an error. - **skipped**: The payout was not triggered as expected.
    * @param limit {@link number } The number of items returned per page.  Default: **10**
    * @return {@link PayoutScheduleExecutions }
    */
    public async getPayoutScheduleExecutions(balanceAccountId: string, id: string, offset: number, results?: Array<ExecutionResult>, limit?: number, requestOptions?: IRequest.Options): Promise<PayoutScheduleExecutions> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/payoutSchedules/{id}/executions`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const hasDefinedQueryParams = results ?? limit ?? offset;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(results) requestOptions.params["results"] = results;
            if(limit) requestOptions.params["limit"] = limit;
            if(offset) requestOptions.params["offset"] = offset;
        }
        const response = await getJsonResponse<string, PayoutScheduleExecutions>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "PayoutScheduleExecutions");
    }

    /**
    * @summary Update a managed payout schedule on a balance account
    * @param balanceAccountId {@link string } The unique identifier of the balance account.
    * @param id {@link string } The unique identifier of the payout schedule applied to the balance account.
    * @param balanceAccountConfigurationUpdate {@link BalanceAccountConfigurationUpdate } 
    * @param requestOptions {@link IRequest.Options }
    * @return {@link BalanceAccountConfiguration }
    */
    public async updateBalanceAccountManagedSchedule(balanceAccountId: string, id: string, balanceAccountConfigurationUpdate: BalanceAccountConfigurationUpdate, requestOptions?: IRequest.Options): Promise<BalanceAccountConfiguration> {
        const endpoint = `${this.baseUrl}/balanceAccounts/{balanceAccountId}/payoutSchedules/{id}`
            .replace("{" + "balanceAccountId" + "}", encodeURIComponent(String(balanceAccountId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const request: BalanceAccountConfigurationUpdate = ObjectSerializer.serialize(balanceAccountConfigurationUpdate, "BalanceAccountConfigurationUpdate");
        const response = await getJsonResponse<BalanceAccountConfigurationUpdate, BalanceAccountConfiguration>(
            resource,
            request,
            { ...requestOptions, method: "PATCH" }
        );

        return ObjectSerializer.deserialize(response, "BalanceAccountConfiguration");
    }

}
