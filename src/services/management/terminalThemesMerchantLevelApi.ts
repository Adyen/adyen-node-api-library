/*
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit this class manually.
 */

import getJsonResponse from "../../helpers/getJsonResponse";
import Service from "../../service";
import Client from "../../client";
import { IRequest } from "../../typings/requestOptions";
import Resource from "../resource";

import { ObjectSerializer } from "../../typings/management/objectSerializer";
import { CreateThemeRequest } from "../../typings/management/models";
import { CreateThemeResponse } from "../../typings/management/models";
import { ListThemesResponse } from "../../typings/management/models";
import { ThemeAsset } from "../../typings/management/models";
import { ThemeUpdateRequest } from "../../typings/management/models";
import { ThemeWithAssetDetails } from "../../typings/management/models";
import { UpdateThemeResponse } from "../../typings/management/models";

/**
 * API handler for TerminalThemesMerchantLevelApi
 */
export class TerminalThemesMerchantLevelApi extends Service {

    private readonly API_BASEPATH: string = "https://management-test.adyen.com/v3";
    private baseUrl: string;

    public constructor(client: Client){
        super(client);
        this.baseUrl = this.createBaseUrl(this.API_BASEPATH);
    }

    /**
    * @summary Add an asset to a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param requestOptions {@link IRequest.Options }
    * @param type {@link &#39;logo&#39; | &#39;homeScreen&#39; | &#39;idleScreen&#39; | &#39;afterPaymentScreen&#39; | &#39;shopperEngagementScreen&#39; } (Required) The screen type or screen element that the asset is for:  - **logo**: your logo. Only supported if &#x60;model&#x60; is **All**. - **homeScreen**: the default background screen. - **idleScreen**: a screensaver shown when the terminal is inactive. Only supported if &#x60;model&#x60; is **SFO1** or **M450**. - **afterPaymentScreen**: a screen shown after the payment is completed. - **shopperEngagementScreen**: a screen shown during a [shopper engagement](https://docs.adyen.com/point-of-sale/shopper-engagement) request. Only supported if &#x60;model&#x60; is **SFO1** or **M450**.
    * @param model {@link &#39;All&#39; | &#39;SFO1&#39; | &#39;M450&#39; | &#39;S1F2&#39; | &#39;S1E2L&#39; | &#39;S1F2L&#39; | &#39;S1F4Pro&#39; | &#39;S1E4Pro&#39; | &#39;S1U2&#39; | &#39;S1U2M4&#39; | &#39;LDN1&#39; | &#39;AMS1&#39; | &#39;P630&#39; } (Required) The Android terminal model that the asset is for. Use **All** only if the &#x60;type&#x60; is **logo**. For every other screen type specify a single Android terminal model such as **AMS1**.
    * @return {@link ThemeAsset }
    */
    public async addTerminalThemeAssetForMerchant(merchantId: string, id: string, file: import("../../typings/httpFile").HttpFile, type: "logo" | "homeScreen" | "idleScreen" | "afterPaymentScreen" | "shopperEngagementScreen", model: "All" | "SFO1" | "M450" | "S1F2" | "S1E2L" | "S1F2L" | "S1F4Pro" | "S1E4Pro" | "S1U2" | "S1U2M4" | "LDN1" | "AMS1" | "P630", requestOptions?: IRequest.Options): Promise<ThemeAsset> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}/assets`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        if (file === null || file === undefined) {
            throw new Error("Required parameter file was null or undefined");
        }
        const contentTypes = ["multipart/form-data"];
        const requestForm = await import("../../helpers/requestForm");
        const form = requestForm.createRequestForm(contentTypes);
        if (file != null) {
            if ("getBuffer" in form) {
                form.append("file", file.data, { filename: file.name });
            }
        }
        const { request, headers } = requestForm.finalizeRequestForm(form, requestOptions?.headers);
        const hasDefinedQueryParams = type ?? model;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(type) requestOptions.params["type"] = type;
            if(model) requestOptions.params["model"] = model;
        }
        const response = await getJsonResponse<string | Buffer, ThemeAsset>(
            resource,
            request,
            { ...requestOptions, headers, method: "POST" }
        );

        return ObjectSerializer.deserialize(response, "ThemeAsset");
    }

    /**
    * @summary Apply a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link void }
    */
    public async applyTerminalThemeForMerchant(merchantId: string, id: string, requestOptions?: IRequest.Options): Promise<void> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}/apply`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        await getJsonResponse<string, void>(
            resource,
            "",
            { ...requestOptions, method: "POST" }
        );
    }

    /**
    * @summary Create a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param createThemeRequest {@link CreateThemeRequest } The theme to create.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link CreateThemeResponse }
    */
    public async createTerminalThemeForMerchant(merchantId: string, createThemeRequest: CreateThemeRequest, requestOptions?: IRequest.Options): Promise<CreateThemeResponse> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)));
        const resource = new Resource(this, endpoint);
        
        const request: CreateThemeRequest = ObjectSerializer.serialize(createThemeRequest, "CreateThemeRequest");
        const response = await getJsonResponse<CreateThemeRequest, CreateThemeResponse>(
            resource,
            request,
            { ...requestOptions, method: "POST" }
        );

        return ObjectSerializer.deserialize(response, "CreateThemeResponse");
    }

    /**
    * @summary Delete an asset from a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param requestOptions {@link IRequest.Options }
    * @param type {@link &#39;logo&#39; | &#39;homeScreen&#39; | &#39;idleScreen&#39; | &#39;afterPaymentScreen&#39; | &#39;shopperEngagementScreen&#39; } (Required) The screen type or screen element that the asset is for:  - **logo**: your logo. Only supported if &#x60;model&#x60; is **All**. - **homeScreen**: the default background screen. - **idleScreen**: a screensaver shown when the terminal is inactive. Only supported if &#x60;model&#x60; is **SFO1** or **M450**. - **afterPaymentScreen**: a screen shown after the payment is completed. - **shopperEngagementScreen**: a screen shown during [shopper engagement](https://docs.adyen.com/point-of-sale/shopper-engagement) requests. Only supported if &#x60;model&#x60; is **SFO1** or **M450**.
    * @param model {@link &#39;All&#39; | &#39;SFO1&#39; | &#39;M450&#39; | &#39;S1F2&#39; | &#39;S1E2L&#39; | &#39;S1F2L&#39; | &#39;S1F4Pro&#39; | &#39;S1E4Pro&#39; | &#39;S1U2&#39; | &#39;S1U2M4&#39; | &#39;LDN1&#39; | &#39;AMS1&#39; | &#39;P630&#39; } (Required) The Android terminal model that the asset is for. Use **All** only if the &#x60;type&#x60; is **logo**. For every other screen type specify a single Android terminal model such as **AMS1**.
    * @return {@link void }
    */
    public async deleteTerminalThemeAssetForMerchant(merchantId: string, id: string, type: "logo" | "homeScreen" | "idleScreen" | "afterPaymentScreen" | "shopperEngagementScreen", model: "All" | "SFO1" | "M450" | "S1F2" | "S1E2L" | "S1F2L" | "S1F4Pro" | "S1E4Pro" | "S1U2" | "S1U2M4" | "LDN1" | "AMS1" | "P630", requestOptions?: IRequest.Options): Promise<void> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}/assets`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const hasDefinedQueryParams = type ?? model;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(type) requestOptions.params["type"] = type;
            if(model) requestOptions.params["model"] = model;
        }
        await getJsonResponse<string, void>(
            resource,
            "",
            { ...requestOptions, method: "DELETE" }
        );
    }

    /**
    * @summary Delete a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link void }
    */
    public async deleteTerminalThemeForMerchant(merchantId: string, id: string, requestOptions?: IRequest.Options): Promise<void> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        await getJsonResponse<string, void>(
            resource,
            "",
            { ...requestOptions, method: "DELETE" }
        );
    }

    /**
    * @summary Replace a theme asset
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param requestOptions {@link IRequest.Options }
    * @param type {@link &#39;logo&#39; | &#39;homeScreen&#39; | &#39;idleScreen&#39; | &#39;afterPaymentScreen&#39; | &#39;shopperEngagementScreen&#39; } (Required) The screen type or screen element that the asset is for:  - **logo**: your logo. Only supported if &#x60;model&#x60; is **All**. - **homeScreen**: the default background screen. - **idleScreen**: a screensaver shown when the terminal is inactive. Only supported if &#x60;model&#x60; is **SFO1** or **M450**. - **afterPaymentScreen**: a screen shown after the payment is completed. - **shopperEngagementScreen**: a screen shown during [shopper engagement](https://docs.adyen.com/point-of-sale/shopper-engagement) requests. Only supported if &#x60;model&#x60; is **SFO1** or **M450**.
    * @param model {@link &#39;All&#39; | &#39;SFO1&#39; | &#39;M450&#39; | &#39;S1F2&#39; | &#39;S1E2L&#39; | &#39;S1F2L&#39; | &#39;S1F4Pro&#39; | &#39;S1E4Pro&#39; | &#39;S1U2&#39; | &#39;S1U2M4&#39; | &#39;LDN1&#39; | &#39;AMS1&#39; | &#39;P630&#39; } (Required) The Android terminal model that the asset is for. Use **All** only if the &#x60;type&#x60; is **logo**. For every other screen type specify a single Android terminal model such as **AMS1**.
    * @return {@link ThemeAsset }
    */
    public async editTerminalThemeAssetForMerchant(merchantId: string, id: string, file: import("../../typings/httpFile").HttpFile, type: "logo" | "homeScreen" | "idleScreen" | "afterPaymentScreen" | "shopperEngagementScreen", model: "All" | "SFO1" | "M450" | "S1F2" | "S1E2L" | "S1F2L" | "S1F4Pro" | "S1E4Pro" | "S1U2" | "S1U2M4" | "LDN1" | "AMS1" | "P630", requestOptions?: IRequest.Options): Promise<ThemeAsset> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}/assets/replace`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        if (file === null || file === undefined) {
            throw new Error("Required parameter file was null or undefined");
        }
        const contentTypes = ["multipart/form-data"];
        const requestForm = await import("../../helpers/requestForm");
        const form = requestForm.createRequestForm(contentTypes);
        if (file != null) {
            if ("getBuffer" in form) {
                form.append("file", file.data, { filename: file.name });
            }
        }
        const { request, headers } = requestForm.finalizeRequestForm(form, requestOptions?.headers);
        const hasDefinedQueryParams = type ?? model;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(type) requestOptions.params["type"] = type;
            if(model) requestOptions.params["model"] = model;
        }
        const response = await getJsonResponse<string | Buffer, ThemeAsset>(
            resource,
            request,
            { ...requestOptions, headers, method: "POST" }
        );

        return ObjectSerializer.deserialize(response, "ThemeAsset");
    }

    /**
    * @summary Get a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link ThemeWithAssetDetails }
    */
    public async getTerminalThemeByIdForMerchant(merchantId: string, id: string, requestOptions?: IRequest.Options): Promise<ThemeWithAssetDetails> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const response = await getJsonResponse<string, ThemeWithAssetDetails>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "ThemeWithAssetDetails");
    }

    /**
    * @summary List terminal themes
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param requestOptions {@link IRequest.Options }
    * @param pageSize {@link number } The number of themes to return per page. If not specified, the list has 10 themes per page.
    * @param page {@link number } The number of the page to fetch. If not specified, the list starts from page 1.
    * @param search {@link string } A search term to filter themes by name. The match is case-insensitive and returns any theme with a name that contains the search term.
    * @return {@link ListThemesResponse }
    */
    public async listTerminalThemesForMerchant(merchantId: string, pageSize?: number, page?: number, search?: string, requestOptions?: IRequest.Options): Promise<ListThemesResponse> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)));
        const resource = new Resource(this, endpoint);
        
        const hasDefinedQueryParams = pageSize ?? page ?? search;
        if(hasDefinedQueryParams) {
            if(!requestOptions) requestOptions = {};
            if(!requestOptions.params) requestOptions.params = {};
            if(pageSize) requestOptions.params["pageSize"] = pageSize;
            if(page) requestOptions.params["page"] = page;
            if(search) requestOptions.params["search"] = search;
        }
        const response = await getJsonResponse<string, ListThemesResponse>(
            resource,
            "",
            { ...requestOptions, method: "GET" }
        );

        return ObjectSerializer.deserialize(response, "ListThemesResponse");
    }

    /**
    * @summary Unapply a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link void }
    */
    public async unapplyTerminalThemeForMerchant(merchantId: string, requestOptions?: IRequest.Options): Promise<void> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/unapply`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)));
        const resource = new Resource(this, endpoint);
        
        await getJsonResponse<string, void>(
            resource,
            "",
            { ...requestOptions, method: "POST" }
        );
    }

    /**
    * @summary Update the metadata of a terminal theme
    * @param merchantId {@link string } The unique identifier of the merchant account.
    * @param id {@link string } The unique identifier of the terminal theme.
    * @param themeUpdateRequest {@link ThemeUpdateRequest } The updated theme metadata.
    * @param requestOptions {@link IRequest.Options }
    * @return {@link UpdateThemeResponse }
    */
    public async updateTerminalThemeForMerchant(merchantId: string, id: string, themeUpdateRequest: ThemeUpdateRequest, requestOptions?: IRequest.Options): Promise<UpdateThemeResponse> {
        const endpoint = `${this.baseUrl}/merchants/{merchantId}/terminalThemes/{id}`
            .replace("{" + "merchantId" + "}", encodeURIComponent(String(merchantId)))
            .replace("{" + "id" + "}", encodeURIComponent(String(id)));
        const resource = new Resource(this, endpoint);
        
        const request: ThemeUpdateRequest = ObjectSerializer.serialize(themeUpdateRequest, "ThemeUpdateRequest");
        const response = await getJsonResponse<ThemeUpdateRequest, UpdateThemeResponse>(
            resource,
            request,
            { ...requestOptions, method: "PATCH" }
        );

        return ObjectSerializer.deserialize(response, "UpdateThemeResponse");
    }

}
